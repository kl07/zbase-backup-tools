To build
----------------

1. Install swig (yum install swig)
2. Install python-devel (yum install python-devel)
3. cd src/vbs_agent
4. Make
5. Install python sendfile
   5a. cd src/pysendfile
   5b. unzip and run setup.py

